// Web stub for `DeviceGeolocationLinux` so that
// `device_geolocation.dart` can be conditionally exported on the web platform
// without pulling in `dart:io`-based dependencies (such as `package:dbus`).
class DeviceGeolocationLinux {
  static void registerWith() {}
}
